CODE SEGMENT
    ASSUME CS:CODE, DS:CODE
LEA SI, 3N
MOV AL, [SI]
REPEAT: CALL SQR
ADD CX, [SNP+BX]
DEC AL
JNZ REPEAT
HLT
SQR PROC 
    PUSH AX
    XOR AH, AH
    MUL AL
    MOV BX, 4
    MOV [SNP+BX], AX
    POP AX
    RET
SQR ENDP
3N DB 7
SNP DW ?
 




